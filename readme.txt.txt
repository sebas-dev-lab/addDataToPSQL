* V-0.0.2

db.config.txt
	- Agregar configuracion de conexion a base de datos postgresql

	- en DB_TABLES=[nombre_de_tablas, ..., ...] => ej: =[judment, users, countries, ...]

> El nombre de cada excel debe coincidir con el nombre de la tabla
> La tabla en PSQL debe estar previamente creada
> La lista configurada arriba debe coincidir con cada nombre de la tabla
> El archivo de configuracion y datos a cargar (.xlsx) deben estar en el mismo directorio que el ejecutable <main>.
